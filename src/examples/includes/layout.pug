<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Hello World example :)
    block styles
      style
        include:scss ../styles/index.scss
  body
    block header
      header.header
        h1 `shader-art` examples
    nav.navigation
      block navigation
        include:markdown-it nav.md
    main
      block main
        shader-art.background(dpr="auto", aria-hidden="true", autoplay="autoplay")
          script(type="vert")
            include:glslminify ../shaders/example-01.vs
          script(type="frag")
            include:glslminify ../shaders/example-01.fs

    block scripts
      script(src="shader-art.umd.min.js")
